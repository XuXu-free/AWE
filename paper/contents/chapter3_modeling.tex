% !TeX root = ../main.tex

\chapter{问题建模、仿真与控制器设计}

本章提出基于Diffusion TCN的安全学习控制框架，用于解决四槽并联AWE系统在实时控制中的功率跟踪与安全约束保证问题。
该框架包含离线学习与在线部署两个阶段：离线阶段以NMPC为专家控制器生成高质量状态-动作数据集，并训练Diffusion TCN策略网络学习专家决策分布；在线阶段通过安全投影算子或控制障碍函数（CBF）对生成策略进行安全修正，确保闭环执行严格满足温度、HTO、电压与功率等关键安全约束。
系统的动力学模型已在第~\ref{chap:modeling}章建立，本章直接沿用其13维状态空间与9维控制输入的通用动力学描述，依次完成专家数据集构建、生成模型策略设计与安全约束保证机制三个核心环节：
首先建立NMPC连续与离散双层时间尺度优化模型，生成覆盖典型工况与极端扰动的专家数据集；
其次设计基于Diffusion TCN的离线学习策略生成器，以条件去噪扩散模型学习专家决策分布；
最后建立安全投影算子与基于CBF的安全约束保证机制，为生成策略提供严格的安全边界。
上述三个环节有机衔接，形成从专家数据采集、策略学习到安全部署的完整技术链条。
\section{基于Diffusion TCN的安全学习控制框架}

所提出的安全学习控制框架分为离线学习与在线部署两个阶段，整体架构如图~\ref{fig:framework} 所示。

\textbf{离线学习阶段}。
以NMPC作为专家控制器，在四槽AWE系统仿真平台上运行闭环控制，生成覆盖典型工况与极端扰动的高质量状态-动作数据集。
该数据集记录了系统在不同功率参考轨迹下的最优控制行为，作为Diffusion TCN策略网络的监督信号。
策略网络以当前状态、上一时刻控制输入与未来功率计划为条件，通过去噪扩散过程学习专家决策分布，输出候选动作序列。

\textbf{在线部署阶段}。
在每个控制周期，策略模型根据实时状态与功率参考采样多条候选动作序列，并通过物理模型推演进行代价评估与加权选择，得到名义控制量。
为确保温度、HTO、电压与功率等安全约束严格满足，名义控制量进一步经过安全投影算子或CBF安全滤波修正，最终形成闭环控制输入作用于AWE系统。
下一时刻状态经传感器反馈回控制器，形成滚动时域闭环。

\section{专家数据集生成}

为训练扩散模型控制策略，需构建覆盖典型工况的专家状态-动作数据集。
本节首先建立NMPC专家控制器的连续时间与离散时间优化模型，然后阐述基于NMPC闭环仿真的数据生成流程与质量控制方法。

\subsection{连续时间NMPC优化问题}

针对$N$槽AWE系统（本研究$N=4$），定义连续时间下的有限时域最优控制问题。
在当前时刻$t_0$，优化目标为在预测时域$T_p = N_p \Delta t$内最小化跟踪误差、温度偏差与控制动作的加权积分：

\begin{align}
  \min_{\mathbf{u}(\cdot)} \quad & J = \int_{t_0}^{t_0+T_p} \Bigg[ \lambda_{track}\left(P_{total}(t) - P_{ref}(t)\right)^2 + \lambda_{temp}\sum_{j=1}^{N}\left(T_{s,j}(t) - T_{ref}\right)^2 \nonumber \\
  & \quad + \lambda_I \sum_{j=1}^{N}\left(\frac{dI_j}{dt}\right)^2 + \lambda_{lye} \sum_{j=1}^{N}v_{lye,j}^2(t) + \lambda_c v_c^2(t) \Bigg] \, dt \label{eq:mpc_continuous} \\
  \text{s.t.} \quad & \dot{\mathbf{x}}(t) = \mathbf{f}\left(\mathbf{x}(t), \mathbf{u}(t)\right), \quad t \in [t_0, t_0+T_p] \nonumber \\
  & \mathbf{g}\left(\mathbf{x}(t), \mathbf{u}(t)\right) \leq 0, \quad t \in [t_0, t_0+T_p] \nonumber \\
  & \mathbf{u}_{min} \leq \mathbf{u}(t) \leq \mathbf{u}_{max} \nonumber
\end{align}

其中$\mathbf{x} \in \mathbb{R}^{13}$为状态向量，$\mathbf{u} = [I_1, \ldots, I_N, v_{lye,1}, \ldots, v_{lye,N}, v_c]^\top \in \mathbb{R}^{2N+1}$为控制输入，$\mathbf{f}(\cdot)$为前文建立的多槽系统连续动力学，$\mathbf{g}(\cdot) \leq 0$包括温度上下限、电压上限、HTO上限与功率上限等路径约束。
电流变化率惩罚项$\lambda_I \sum (dI_j/dt)^2$用于抑制控制抖动，保证执行机构寿命。

\subsection{系统动力学离散化}

为在数值优化框架中求解上述连续时间问题，需对系统动力学进行离散化处理。
NMPC求解涉及两个不同的时间尺度，参考图~\ref{fig:time_scales} 所示的双层时间结构：

\begin{itemize}
  \item \textbf{控制步长}（Control Interval）：$\Delta t$，表示NMPC优化问题中相邻控制输入之间的时间间隔，同时也是实际控制器的执行周期
  \item \textbf{仿真步长}（Simulation Step）：$\delta t$，表示ODE系统动态数值积分的时间步长
  \item \textbf{预测时域}（Prediction Horizon）：$N_p$，表示NMPC向前预测的控制步数
  \item \textbf{每步子步数}：$N_s = \Delta t / \delta t$，表示每个控制步长内包含的仿真子步数
\end{itemize}

\begin{figure}[!htp]
  \centering
  \begin{tikzpicture}[
    box/.style={rectangle, draw, fill=blue!10, minimum width=1.2cm, minimum height=0.6cm, font=\small},
    subbox/.style={rectangle, draw, fill=orange!10, minimum width=0.4cm, minimum height=0.4cm, font=\tiny},
    arrow/.style={->, >=stealth, thick},
    label/.style={font=\small}
  ]
    % 控制步
    \foreach \i in {0,1,2,3,4,5} {
      \node[box] (k\i) at (\i*2.5,0) {$k+\i$};
    }

    % 仿真子步 - 以第一个控制步为例
    \foreach \j in {0,1,...,4} {
      \node[subbox] (s\j) at (0.3+\j*0.4,-1.5) {};
    }
    \node[label] at (1.25,-1.5) {$\cdots$};
    \foreach \j in {295,296,297,298,299} {
      \pgfmathsetmacro{\xpos}{1.7+(\j-295)*0.4}
      \node[subbox] at (\xpos,-1.5) {};
    }

    % 标注
    \draw[arrow] (k0.south) -- ++(0,-0.5) node[left, label] {$\mathbf{u}_{k|k}$} -- ++(0,-0.3);
    \draw[arrow] (k1.south) -- ++(0,-0.5) node[left, label] {$\mathbf{u}_{k+1|k}$} -- ++(0,-0.3);

    % 时间轴标注
    \draw[<->, thick] (k0.north) -- (k1.north) node[midway, above, label] {$\Delta t$};
    \draw[<->, thick] (0.3,-2.2) -- (0.7,-2.2) node[midway, below, label] {$\delta t$};

    % 说明
    \node[label] at (6.25,0.8) {控制步（$N_p$）};
    \node[label] at (6.25,-1.5) {仿真子步（$N_s$步/$\Delta t$）};

  \end{tikzpicture}
  \caption{NMPC双层时间尺度结构示意图}
  \label{fig:time_scales}
\end{figure}

AWE系统的连续时间动力学由非线性常微分方程描述：
\begin{equation}
  \dot{\mathbf{x}}(t) = \mathbf{f}(\mathbf{x}(t), \mathbf{u}(t), \mathbf{p}(t))
\end{equation}

为在NMPC优化框架中处理该连续动力学，使用前向Euler方法离散化。对于第$k$个控制步（在当前时刻$k$的预测时域内），在第$j$个仿真子步的状态更新为：
\begin{equation}
  \mathbf{x}_{k+(j+1)\frac{\delta t}{\Delta t}|k} = \mathbf{x}_{k+j\frac{\delta t}{\Delta t}|k} + \mathbf{f}\left(\mathbf{x}_{k+j\frac{\delta t}{\Delta t}|k}, \mathbf{u}_{k|k}, \mathbf{p}_{k|k}\right) \cdot \delta t
  \label{eq:ode_discretization}
\end{equation}
其中 $j = 0, 1, 2, \ldots, N_s-1$，$N_s = \Delta t / \delta t$。

采用零阶保持假设，控制输入在整个控制步长内保持恒定：
\begin{equation}
  \mathbf{u}(t) = \mathbf{u}_{k|k}, \quad \forall t \in [k\Delta t, (k+1)\Delta t)
\end{equation}

经过$N_s$个仿真子步后，状态推进至下一个控制步，如式~\eqref{eq:state_transition} 所示：
\begin{equation}
  \mathbf{x}_{k+1|k} = \mathbf{x}_{k|k} + \sum_{j=1}^{N_s} \mathbf{f}\left(\boldsymbol{\chi}_{j-1,k}, \mathbf{u}_{k|k}, \mathbf{p}_{k|k}\right) \cdot \delta t
  \label{eq:state_transition}
\end{equation}

其中$\boldsymbol{\chi}_{j,k} = \mathbf{x}_{k+j\frac{\delta t}{\Delta t}|k}$表示在第$k$个控制步内第$j$个仿真子步的中间状态。式~\eqref{eq:ode_discretization} 和式~\eqref{eq:state_transition} 共同构成了NMPC中的双层时间尺度状态转移模型。

\subsection{离散NMPC优化问题}

将连续时间问题~\eqref{eq:mpc_continuous} 经上述离散化后，在当前时刻$k$的离散NMPC优化问题为：

\begin{align}
  \min_{\mathbf{U}_k} \quad & J = \sum_{i=0}^{N_p-1} \Bigg[ \lambda_{track}\left(P_{total,k+i|k} - P_{ref,k+i}\right)^2 + \lambda_{temp}\sum_{j=1}^{N}\left(T_{s,j,k+i|k} - T_{ref}\right)^2 \nonumber \\
  & \quad + \lambda_I \sum_{j=1}^{N}\left(I_{j,k+i|k} - I_{j,k+i-1|k}\right)^2 + \lambda_{lye} \sum_{j=1}^{N}v_{lye,j,k+i|k}^2 + \lambda_c v_{c,k+i|k}^2 \Bigg] \\
  \text{s.t.} \quad & \mathbf{g}\left(\mathbf{x}_{k+i|k}, \mathbf{u}_{k+i|k}\right) \leq 0, \quad i = 0, 1, \ldots, N_p-1 \\
  & \mathbf{x}_{k+i+1|k} - \mathbf{x}_{k+i|k} - \sum_{m=1}^{N_s} \mathbf{f}\left(\boldsymbol{\chi}_{m-1,k+i}, \mathbf{u}_{k+i|k}\right) \delta t = 0, \quad i = 0, 1, \ldots, N_p-1
\end{align}

其中优化变量为控制序列：
\begin{equation}
  \mathbf{U}_k = \left\{\mathbf{u}_{k|k}, \mathbf{u}_{k+1|k}, \ldots, \mathbf{u}_{k+N_p-1|k}\right\}
\end{equation}

每个控制向量包含以下分量：
\begin{equation}
  \mathbf{u}_{k+i|k} = \left[I_{1,k+i|k}, \ldots, I_{N,k+i|k}, v_{lye,1,k+i|k}, \ldots, v_{lye,N,k+i|k}, v_{c,k+i|k}\right]^\top
\end{equation}

不等式约束$\mathbf{g}(\cdot) \leq 0$包括：
\begin{itemize}
  \item 温度约束：$T_{min} \leq T_{s,j,k+i|k} \leq T_{max}, \quad \forall j = 1, \ldots, N$
  \item 电压约束：$U_{cell,j,k+i|k} \leq U_{cell,max}$
  \item HTO约束：$\mathrm{HTO}_{k+i|k} \leq \mathrm{HTO}_{\mathrm{max}}$
  \item 功率约束：$P_{stack,j,k+i|k} \leq P_{stack,max}$
\end{itemize}

\subsection{NMPC求解实现}

NMPC控制器借助CasADi符号计算框架建立优化模型，并由IPOPT求解器进行数值求解。关键实现细节包括：

\begin{itemize}
  \item \textbf{子步进}：每个控制周期内使用$N_s$个仿真子步（步长$\delta t$）确保ODE数值积分稳定性
  \item \textbf{Warm-starting}：利用上一时刻的解作为初始猜测，加速收敛
  \item \textbf{稀疏雅可比}：利用CasADi自动微分生成稀疏雅可比矩阵，提高大规模优化问题求解效率
\end{itemize}

NMPC虽然能够获得高质量的优化控制序列，但其单次求解耗时2--5秒，难以满足AWE系统的实时控制需求，且存在局部最优风险与参数调节复杂等问题。因此，本文将其作为离线专家控制器，用于生成训练数据，而非直接在线部署。

\subsection{数据生成流程}

专家数据集通过两类互补的功率轨迹生成，以确保覆盖真实运行场景与极端扰动工况。

\textbf{第一类：风电功率轨迹}。
采用真实风电场出力曲线作为功率参考，反映AWE系统在实际可再生能源波动场景下的跟踪需求。
风电数据代表了真实运行环境，但功率变化相对连续，系统普遍处于跟踪误差较小的稳态附近。

\textbf{第二类：带噪声阶跃实验数据}。
为补充风电数据难以覆盖的大误差场景，设计多平台阶跃功率信号并叠加随机噪声，人为制造功率骤升、骤降与宽频扰动。
这类实验数据能够扩张专家数据集的状态-动作空间覆盖范围，补充NMPC在较大跟踪误差和温度偏差下的控制策略表现，使学习得到的策略具备更强的鲁棒性。

两类数据互为补充：风电数据保证策略在真实场景下的适用性，带噪声阶跃数据则强化策略在非稳态与极端工况下的泛化能力。

在每个控制周期$k$，以系统当前状态$\mathbf{x}_k$、上一时刻控制输入$\mathbf{u}_{k-1}$和参考功率序列$\{P_{ref,k},\ldots,P_{ref,k+N_p-1}\}$为输入，求解NMPC得到最优控制序列$\mathbf{U}_k^\star$。
将首步控制$\mathbf{u}_k^\star$施加到多时间尺度仿真器，在该周期内通过$N_s$个子步对连续动力学数值积分，得到下一周期状态$\mathbf{x}_{k+1}$。
重复上述过程直至完成整段功率轨迹的闭环仿真。NMPC专家数据生成的完整流程如算法~\ref{algo:expert_data_generation} 所示。

\begin{algorithm}[htb]
  \caption{NMPC专家数据生成}
  \label{algo:expert_data_generation}
  \SetAlgoLined
  \KwIn{功率轨迹集合$\{P_{ref}^{(m)}\}_{m=1}^M$，初始状态分布$p(\mathbf{x}_0)$}
  \KwOut{专家数据集$\mathcal{D}$}
  初始化：$\mathcal{D} \leftarrow \emptyset$\\
  \For{每段功率轨迹$m = 1, \ldots, M$}{
    从$p(\mathbf{x}_0)$采样初始状态$\mathbf{x}_0^{(m)}$\\
    \For{每个控制周期$k = 0, \ldots, K-1$}{
      构造功率计划$P_{ref,k:k+N_p-1}^{(m)}$\\
      求解NMPC优化问题，得到最优控制序列$\mathbf{U}_k^\star$\\
      \eIf{NMPC收敛}{
        记录专家样本：$\mathcal{D} \leftarrow \mathcal{D} \cup \{(\mathbf{x}_k, \mathbf{u}_{k-1}, P_{ref,k:k+N_p-1}^{(m)}, \mathbf{u}_k^\star)\}$\\
        执行首步控制$\mathbf{u}_k^\star$，仿真推进至$\mathbf{x}_{k+1}$\\
      }{
        跳过当前样本或采用上一时刻可行解回退\\
      }
    }
  }
  对$\mathcal{D}$进行归一化与训练/测试集划分\\
  \Return{$\mathcal{D}$}
\end{algorithm}

生成过程中记录样本：
\begin{equation}
  \mathcal{D} = \left\{(\mathbf{o}_k, \mathbf{u}_k^\star)\right\}_{k=0}^{K-1}, \quad \mathbf{o}_k = [\mathbf{x}_k, \mathbf{u}_{k-1}, P_{ref,k:k+N_p-1}]
\end{equation}
其中$\mathbf{o}_k$为策略条件输入，包含当前状态、上一时刻控制输入与未来$N_p$步的功率计划，$\mathbf{u}_k^\star$为NMPC给出的专家动作。

\subsection{场景覆盖与数据质量控制}

为增强数据集的代表性，专家数据生成覆盖多段功率轨迹与多组初始条件。
每条轨迹开始时，对关键状态（如各槽温度、分离器状态量）进行扰动初始化，以覆盖不同热态与杂质累积水平。
为保证样本质量，若NMPC在个别时刻未能收敛，则跳过该时刻的样本或采用上一时刻可行解作为回退控制，以避免引入明显的异常标签。

\subsection{数据格式与预处理}

训练数据以控制周期为采样间隔，输入为$\mathbf{o}_k$，监督标签为$\mathbf{u}_k^\star$。
对各维状态与控制量采用按维线性归一化或标准化处理，使不同量纲的变量在网络训练中具有可比尺度。
数据集按轨迹划分为训练集与测试集，以避免同一条闭环轨迹在不同划分间造成信息泄漏。

\section{DiffusionTCN离线学习策略生成器}

\subsection{扩散模型策略（Diffusion Policy）}

去噪扩散概率模型（Denoising Diffusion Probabilistic Model, DDPM）是一类生成模型，通过学习逆转一个逐步加噪的扩散过程生成高质量数据。
其核心思想来源于非平衡热力学中的扩散过程，通过马尔可夫链将数据逐步转化为噪声，再学习逆过程从噪声恢复数据。

\textbf{1. 前向扩散过程}

前向过程是固定的马尔可夫链，从原始数据$\mathbf{x}_0$开始，逐步添加高斯噪声，经过$T$步后得到纯噪声$\mathbf{x}_T \sim \mathcal{N}(0, \mathbf{I})$。每步的加噪过程定义为：
\begin{equation}
  q(\mathbf{x}_t | \mathbf{x}_{t-1}) = \mathcal{N}(\mathbf{x}_t; \sqrt{1-\beta_t}\mathbf{x}_{t-1}, \beta_t \mathbf{I})
\end{equation}

其中$\beta_t \in (0, 1)$是预先定义的方差调度（variance schedule），控制每步添加的噪声量。通常采用线性或余弦调度，确保$\beta_1 < \beta_2 < \cdots < \beta_T$。

利用重参数化技巧，可以直接从$\mathbf{x}_0$采样任意时间步$t$的状态：
\begin{equation}
  \mathbf{x}_t = \sqrt{\bar{\alpha}_t}\mathbf{x}_0 + \sqrt{1-\bar{\alpha}_t}\boldsymbol{\epsilon}, \quad \boldsymbol{\epsilon} \sim \mathcal{N}(0, \mathbf{I})
\end{equation}

其中$\alpha_t = 1 - \beta_t$，$\bar{\alpha}_t = \prod_{s=1}^{t}\alpha_s$。当$T$足够大时，$\mathbf{x}_T$近似服从标准正态分布。

\textbf{2. 反向去噪过程}

反向过程是学习得到的马尔可夫链，从纯噪声$\mathbf{x}_T$开始，逐步去噪恢复数据。理论上，当$\beta_t$足够小时，反向过程也是高斯分布：
\begin{equation}
  p_\theta(\mathbf{x}_{t-1} | \mathbf{x}_t) = \mathcal{N}(\mathbf{x}_{t-1}; \boldsymbol{\mu}_\theta(\mathbf{x}_t, t), \boldsymbol{\Sigma}_\theta(\mathbf{x}_t, t))
\end{equation}

其中均值$\boldsymbol{\mu}_\theta$和方差$\boldsymbol{\Sigma}_\theta$由神经网络参数化。

根据贝叶斯定理，后验分布$q(\mathbf{x}_{t-1} | \mathbf{x}_t, \mathbf{x}_0)$也是高斯分布，其均值为：
\begin{equation}
  \tilde{\boldsymbol{\mu}}_t(\mathbf{x}_t, \mathbf{x}_0) = \frac{\sqrt{\bar{\alpha}_{t-1}}\beta_t}{1-\bar{\alpha}_t}\mathbf{x}_0 + \frac{\sqrt{\alpha_t}(1-\bar{\alpha}_{t-1})}{1-\bar{\alpha}_t}\mathbf{x}_t
\end{equation}

\textbf{3. 噪声预测网络}

DDPM的核心思想是：直接预测原始数据$\mathbf{x}_0$比较困难，而预测噪声$\boldsymbol{\epsilon}$更具效率。
因此，神经网络$\boldsymbol{\epsilon}_\theta(\mathbf{x}_t, t, c)$被训练来预测前向过程中添加的噪声，其中$c$为条件信息（如系统状态和功率计划）。

将$\mathbf{x}_0 = \frac{\mathbf{x}_t - \sqrt{1-\bar{\alpha}_t}\boldsymbol{\epsilon}}{\sqrt{\bar{\alpha}_t}}$代入后验均值表达式，得到：
\begin{equation}
  \boldsymbol{\mu}_\theta(\mathbf{x}_t, t) = \frac{1}{\sqrt{\alpha_t}}\left(\mathbf{x}_t - \frac{1-\alpha_t}{\sqrt{1-\bar{\alpha}_t}}\boldsymbol{\epsilon}_\theta(\mathbf{x}_t, t, c)\right)
\end{equation}

\textbf{4. 训练目标}

简化的训练目标为预测噪声的均方误差：
\begin{equation}
  \mathcal{L} = \mathbb{E}_{t, \mathbf{x}_0, \boldsymbol{\epsilon}} \left[ \| \boldsymbol{\epsilon} - \boldsymbol{\epsilon}_\theta(\mathbf{x}_t, t, c) \|^2 \right]
\end{equation}

其中$t$从均匀分布$\mathcal{U}(\{1, \ldots, T\})$中采样，$\boldsymbol{\epsilon} \sim \mathcal{N}(0, \mathbf{I})$。该目标在实践中兼具简洁性与高效性，表现良好。

\textbf{5. 训练流程}

Diffusion TCN的训练过程如算法~\ref{algo:diffusion_training} 所示。训练前，首先对专家数据集中的状态和动作进行Min-Max归一化，使各维度数值处于相近量级，以加速网络收敛。在每个训练迭代中，从数据集中随机采样一个批次专家样本$\{(\mathbf{x}_0^{(i)}, c^{(i)})\}_{i=1}^B$，并独立采样对应的时间步$t^{(i)} \sim \mathcal{U}(\{1, \ldots, T\})$与标准高斯噪声$\boldsymbol{\epsilon}^{(i)} \sim \mathcal{N}(0, \mathbf{I})$。根据前向扩散的重参数化公式计算加噪样本$\mathbf{x}_{t^{(i)}}^{(i)}$，输入噪声预测网络得到$\boldsymbol{\epsilon}_\theta(\mathbf{x}_{t^{(i)}}^{(i)}, t^{(i)}, c^{(i)})$，并以均方误差作为损失函数进行梯度下降更新。该训练流程无需对完整扩散链进行显式建模，仅通过单步噪声预测即可有效学习专家决策分布。

\begin{algorithm}[htb]
  \caption{Diffusion TCN训练流程}
  \label{algo:diffusion_training}
  \SetAlgoLined
  \KwIn{专家数据集$\mathcal{D}$，扩散步数$T$，批大小$B$，学习率$\eta$，训练轮数$E$}
  \KwOut{训练好的噪声预测网络参数$\theta$}
  对$\mathcal{D}$中的状态与动作进行Min-Max归一化\\
  初始化网络参数$\theta$\\
  \For{$epoch = 1, \ldots, E$}{
    随机打乱数据集$\mathcal{D}$\\
    \For{每个批次$\{(\mathbf{x}_0^{(i)}, c^{(i)})\}_{i=1}^B \subset \mathcal{D}$}{
      采样时间步$t^{(i)} \sim \mathcal{U}(\{1, \ldots, T\})$\\
      采样噪声$\boldsymbol{\epsilon}^{(i)} \sim \mathcal{N}(0, \mathbf{I})$\\
      计算加噪样本：$\mathbf{x}_{t^{(i)}}^{(i)} = \sqrt{\bar{\alpha}_{t^{(i)}}}\mathbf{x}_0^{(i)} + \sqrt{1-\bar{\alpha}_{t^{(i)}}}\boldsymbol{\epsilon}^{(i)}$\\
      预测噪声：$\hat{\boldsymbol{\epsilon}}^{(i)} = \boldsymbol{\epsilon}_\theta(\mathbf{x}_{t^{(i)}}^{(i)}, t^{(i)}, c^{(i)})$\\
      计算损失：$\mathcal{L} = \frac{1}{B}\sum_{i=1}^B \|\boldsymbol{\epsilon}^{(i)} - \hat{\boldsymbol{\epsilon}}^{(i)}\|^2$\\
      梯度下降更新：$\theta \leftarrow \theta - \eta \nabla_\theta \mathcal{L}$\\
    }
  }
  \Return{$\theta$}
\end{algorithm}

\textbf{6. 推理采样}

训练完成后，从标准正态分布采样$\mathbf{x}_T \sim \mathcal{N}(0, \mathbf{I})$，然后迭代执行去噪步骤：
\begin{equation}
  \mathbf{x}_{t-1} = \frac{1}{\sqrt{\alpha_t}}\left(\mathbf{x}_t - \frac{1-\alpha_t}{\sqrt{1-\bar{\alpha}_t}}\boldsymbol{\epsilon}_\theta(\mathbf{x}_t, t, c)\right) + \sqrt{\beta_t}\mathbf{z}
\end{equation}

其中$\mathbf{z} \sim \mathcal{N}(0, \mathbf{I})$（当$t > 1$时，$\mathbf{z} = \mathbf{0}$）。经过$T$步迭代后，得到生成的动作序列$\mathbf{x}_0$。

\textbf{6. 扩散模型用于控制的优势}

将扩散模型应用于控制任务具有以下优势：
\begin{itemize}
  \item \textbf{多模态建模}：捕捉控制动作的多峰分布，而非单点估计
  \item \textbf{高质量样本}：生成的动作序列平滑且符合物理约束
  \item \textbf{条件生成}：通过条件信息$c$实现上下文相关的动作生成
  \item \textbf{灵活性}：可以与不同的采样策略（如分类器引导、代价加权）结合
\end{itemize}

\textbf{7. 预测控制中的应用}

在预测控制框架中，扩散模型用于生成候选动作序列：
\begin{equation}
  \mathbf{a}_{t:t+N_p} \sim \pi_\theta(\cdot | \mathbf{x}_t, P_{ref,t:t+N_p})
\end{equation}

其中条件$c = [\mathbf{x}_t, P_{ref,t:t+N_p}]$包含当前状态和未来$N_p$步的功率计划。通过代价加权选择机制，从多个候选序列中选择最优动作执行。

在线部署阶段的推理架构如图~\ref{fig:framework} 所示：策略网络以当前状态、上一时刻动作和未来功率计划为条件，通过去噪扩散过程生成多条候选动作序列，再经代价加权选择得到名义控制量，最终经安全修正环节输出闭环控制输入。

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.98\textwidth]{figures/control_structure.png}
  \caption{基于Diffusion TCN的安全学习控制框架}
  \label{fig:framework}
\end{figure}

\subsection{网络架构}

\subsubsection{时序卷积网络（TCN）}

采用3层空洞卷积结构：
\begin{itemize}
  \item 隐藏维度：256
  \item 卷积核大小：3
  \item 空洞因子：$[1, 2, 4]$
  \item 激活函数：Mish
  \item 时间步嵌入：正弦位置编码
\end{itemize}

图~\ref{fig:network_arch} 展示了Diffusion TCN的网络架构。

% network_structure.png Diffusion TCN网络架构图
\begin{figure}[!htp]
  \centering
  \includegraphics[width=0.98\textwidth]{figures/network_structure.png}
  \caption{Diffusion TCN网络架构图}
  \label{fig:network_arch}
\end{figure}

网络接收噪声输入、时间步和条件信息，通过输入投影、时间嵌入和条件投影进行特征变换，再经过3层空洞卷积（dilation分别为1、2、4）的TCN块处理，输出预测的噪声或速度场。
右侧详细展示了TemporalBlock的内部结构，包含两层空洞因果卷积、Mish激活、Dropout以及残差连接。
当维度不匹配时，通过$1\times1$卷积完成通道对齐。

具体而言，噪声输入$\mathbf{x}\in\mathbb{R}^{N\times T\times C_{in}}$对应动作序列的噪声表示，
时间步$t\in\mathbb{R}^{N\times 1}$经过MLP映射得到时间特征，
条件输入$\mathbf{c}\in\mathbb{R}^{N\times T\times C_{cond}}$通过一维卷积投影到隐藏维度并与主干特征融合。

TCN的核心优势在于利用空洞卷积（Dilated Convolution）扩大感受野，其有效范围随网络深度呈指数级增长：
\begin{equation}
  \mathrm{RF} = k^L = 3^3 = 27
\end{equation}
其中$k=3$为卷积核大小，$L=3$为层数。网络因而可捕获长时序依赖关系，适合预测控制中的时序动作生成任务。

图~\ref{fig:tcn_diffusion_training} 展示了训练过程的计算流。

\begin{figure}[!htp]
  \centering
  \begin{tikzpicture}[
    block/.style={rectangle, draw, rounded corners, minimum width=2.5cm, minimum height=0.8cm, align=center, font=\small},
    smallblock/.style={rectangle, draw, rounded corners, minimum width=1.8cm, minimum height=0.6cm, align=center, font=\small},
    arrow/.style={->, >=stealth, thick},
    label/.style={font=\small}
  ]
    % 输入
    \node[block, fill=green!15] (x1) at (0,3) {专家动作\\$\mathbf{a}_0$};
    \node[block, fill=blue!15] (cond) at (0,1) {条件\\$[\mathbf{x}, P_{ref}]$};
    \node[smallblock, fill=gray!20] (sample) at (2.5,4) {采样\\$t \sim U[0,1]$};
    \node[smallblock, fill=gray!30] (noise) at (2.5,3) {加噪\\$\mathbf{x}_t$};
    \node[smallblock, fill=gray!30] (eps) at (2.5,2) {噪声\\$\boldsymbol{\epsilon}$};

    % 网络
    \node[block, fill=orange!25, minimum width=3cm] (net) at (6,2.5) {TCN Diffusion\\网络$\epsilon_\theta$};

    % 输出和损失
    \node[smallblock, fill=red!15] (pred) at (9.5,2.5) {预测噪声\\$\hat{\boldsymbol{\epsilon}}$};
    \node[smallblock, fill=red!25] (loss) at (12,2.5) {MSE损失};

    % 箭头
    \draw[arrow] (x1) -- (noise);
    \draw[arrow] (noise) -- (net);
    \draw[arrow] (cond) -- (net);
    \draw[arrow] (sample) -- (noise);
    \draw[arrow] (eps) -- (loss);
    \draw[arrow] (net) -- (pred);
    \draw[arrow] (pred) -- (loss);

    % 标注
    \node[label] at (6,0.3) {训练目标：$\mathcal{L} = \mathbb{E}\left[\|\boldsymbol{\epsilon} - \epsilon_\theta(\mathbf{x}_t, t, c)\|^2\right]$};
  \end{tikzpicture}
  \caption{Diffusion TCN训练过程示意图（DDPM）}
  \label{fig:tcn_diffusion_training}
\end{figure}

\subsubsection{多层感知机（MLP）}

MLP作为对比基线，采用全连接残差网络结构：
\begin{itemize}
  \item 隐藏层维度：$[512, 512, 512]$
  \item 残差连接
  \item LayerNorm归一化
\end{itemize}

与TCN相比，MLP将整条动作序列展平为固定长度的向量后回归预测，
缺乏对时间维度显式建模的能力，生成的动作序列在时间维度的平滑性相对较弱。
但由于其结构简单、参数量较小，仍可作为验证时序网络必要性的对照基线。

生成模型输出候选动作序列后，通过物理仿真评估代价，其控制流程如算法~\ref{algo:generative_control} 所示。

\begin{algorithm}[htb]
  \caption{生成模型控制策略}
  \label{algo:generative_control}
  \SetAlgoLined
  \KwIn{当前状态$\mathbf{x}_t$，功率计划$P_{ref}$，策略模型$\pi_\theta$，采样数$K$}
  \KwOut{控制动作$\mathbf{a}_t$}

  % 采样候选序列
  采样$K$条候选动作序列$\{\mathbf{a}^{(i)}\}_{i=1}^K \sim \pi_\theta(\mathbf{x}_t, P_{ref})$

  % 评估每条序列
  \For{$i = 1, \ldots, K$}{
    使用物理模型推演轨迹$\boldsymbol{\tau}^{(i)}$

    计算综合代价：
    $J^{(i)} = \lambda_{track}J_{track}^{(i)} + \lambda_{temp}J_{temp}^{(i)} + \lambda_{I}J_{I}^{(i)} + \lambda_{lye}J_{lye}^{(i)} + \lambda_{c}J_{c}^{(i)}$
  }

  % 加权选择
  计算选择概率：$p^{(i)} \propto \exp(-J^{(i)}/\alpha)$

  采样选择最优动作索引：$i^* \sim \text{Categorical}(\mathbf{p})$

  提取首步动作：$\mathbf{a}_t = \mathbf{a}^{(i^*)}_0$

  \Return{$\mathbf{a}_t$}
\end{algorithm}

\section{安全约束保证机制}

\subsection{安全投影算子}

为使生成控制量始终处于安全范围，建立安全投影算子（Safety Projection Operator），将无约束生成模型输出的名义控制量修正到可行控制集内。

\subsubsection{可行控制集的定义}

给定当前状态$\mathbf{x}_t$，定义可行控制集$\mathcal{C}(\mathbf{x}_t)$为同时满足状态相关安全约束与输入饱和约束的所有控制输入集合：
\begin{equation}
  \mathcal{C}(\mathbf{x}_t) = \left\{\mathbf{u}\ \middle|\ \mathbf{g}(\mathbf{x}_t,\mathbf{u}) \le 0,\ \mathbf{u}_{min} \le \mathbf{u} \le \mathbf{u}_{max}\right\}
\end{equation}
其中$\mathbf{g}(\mathbf{x}_t,\mathbf{u}) \le 0$ 为单步预测安全约束，具体包括：
\begin{itemize}
  \item \textbf{温度约束}：$T_{min} \le T_{s,i}(\mathbf{x}_t, \mathbf{u}) \le T_{max},\ i=1,\dots,N$
  \item \textbf{HTO约束}：$\mathrm{HTO}(\mathbf{x}_t, \mathbf{u}) \le \mathrm{HTO}_{\mathrm{max}}$
  \item \textbf{电压约束}：$U_{cell,i}(\mathbf{x}_t, \mathbf{u}) \le U_{cell,max},\ i=1,\dots,N$
  \item \textbf{功率约束}：$P_{i}(\mathbf{x}_t, \mathbf{u}) \le P_{stack,max},\ i=1,\dots,N$
\end{itemize}
以上约束中的下一时刻状态量由简化的热力学模型单步预测得到。以电解槽温度为例，采用前向Euler近似：
\begin{equation}
  T_{s,i}(\mathbf{x}_t, \mathbf{u}) \approx T_{s,i,t} + \frac{\Delta t}{C_s}\left[ Q_{ele,i} - Q_{diss,i} - Q_{flow,i} \right]
\end{equation}
其中$\Delta t$为控制周期，热生成项$Q_{ele,i}$、热耗散项$Q_{diss,i}$与对流换热项$Q_{flow,i}$均采用当前状态$\mathbf{x}_t$和候选控制$\mathbf{u}$计算。HTO、电压与功率的单步预测亦采用相同原理。

\subsubsection{投影优化问题}

投影后的安全控制量为名义控制量$\mathbf{u}_{nom}$到可行集$\mathcal{C}(\mathbf{x}_t)$上的欧几里得投影：
\begin{equation}
  \mathbf{u}_t = \mathcal{P}_{\mathcal{C}(\mathbf{x}_t)}(\mathbf{u}_{nom}) = \arg\min_{\mathbf{u}\in\mathcal{C}(\mathbf{x}_t)} \|\mathbf{u}-\mathbf{u}_{nom}\|_2^2
\label{eq:safety_projection}
\end{equation}
该优化问题的目标函数为二次型，但约束包含非线性单步预测约束与盒式输入约束，因此属于非线性规划（NLP）问题，而非标准二次规划（QP）。

\subsubsection{数值求解实现}

实现了两套数值求解器以兼顾精度与效率：
\begin{itemize}
  \item \textbf{CasADi/IPOPT}：利用CasADi符号计算框架自动求导，IPOPT内点法求解，适用于对精度要求较高的离线验证；
  \item \textbf{SciPy SLSQP}：基于SciPy优化库的序列最小二乘规划算法，无需额外符号计算依赖，适用于快速在线投影。
\end{itemize}

投影算子的执行流程如算法~\ref{algo:safety_projection} 所示。

\begin{algorithm}[htb]
  \caption{安全投影算子}
  \label{algo:safety_projection}
  \SetAlgoLined
  \KwIn{当前状态$\mathbf{x}_t$，名义控制量$\mathbf{u}_{nom}$}
  \KwOut{安全控制量$\mathbf{u}_t$}
  初始化：设定可行控制集$\mathcal{C}(\mathbf{x}_t)$\\
  \For{温度、HTO、电压、功率约束$j=1,\dots,m$}{
    构建单步预测约束$g_j(\mathbf{x}_t, \mathbf{u}) \le 0$\\
  }
  求解带约束优化问题~\eqref{eq:safety_projection}：\\
  \quad$\mathbf{u}_t = \arg\min_{\mathbf{u}\in\mathcal{C}(\mathbf{x}_t)} \|\mathbf{u}-\mathbf{u}_{nom}\|_2^2$\\
  \Return{$\mathbf{u}_t$}
\end{algorithm}

该投影修正可在不显著偏离生成策略的前提下，严格满足温度、HTO、电压与功率等关键安全约束。

\subsection{基于控制障碍函数的安全约束保证}

控制障碍函数（Control Barrier Function, CBF）为安全约束提供了严格的理论保证~\cite{Ames2019CBF}。
本节针对AWE系统的HTO约束，建立非线性非仿射CBF框架，给出完整的李导数推导。

\subsubsection{CBF基本概念}

考虑一般非线性控制系统$\dot{\mathbf{x}} = \mathbf{f}(\mathbf{x}, \mathbf{u})$，其中$\mathbf{x}\in\mathbb{R}^n$，$\mathbf{u}\in\mathcal{U}\subset\mathbb{R}^m$。设安全集$\mathcal{C}$定义为：
\begin{equation}
  \mathcal{C} = \left\{ \mathbf{x}\in\mathbb{R}^n \mid h(\mathbf{x}) \ge 0 \right\}
\end{equation}
其中$h: \mathbb{R}^n \to \mathbb{R}$为连续可微函数。若存在类$\mathcal{K}$函数$\alpha(\cdot)$使得对所有$\mathbf{x}\in\mathcal{C}$有：
\begin{equation}
  \sup_{\mathbf{u}\in\mathcal{U}} \left[ \frac{\partial h}{\partial \mathbf{x}} \mathbf{f}(\mathbf{x}, \mathbf{u}) \right] \ge -\alpha(h(\mathbf{x}))
\end{equation}
则称$h$为控制障碍函数。取$\alpha(h) = \gamma_{CBF} h$（$\gamma_{CBF}>0$），得到广泛采用的指数型CBF条件：
\begin{equation}
  \dot{h}(\mathbf{x}, \mathbf{u}) + \gamma_{CBF} h(\mathbf{x}) \ge 0
\label{eq:cbf_condition}
\end{equation}
式~\eqref{eq:cbf_condition} 保证了安全集$\mathcal{C}$的前向不变性：若$\mathbf{x}(0)\in\mathcal{C}$，则对所有$t\ge 0$有$\mathbf{x}(t)\in\mathcal{C}$。

\subsubsection{李导数计算}

以HTO约束为例，推导其CBF李导数。根据理想气体状态方程$PV=nRT$，气相中氢气的体积百分比为：
\begin{equation}
  HTO_{pct} = \frac{n_{gas} \cdot R \cdot T_{sep}}{P_{sys} \cdot V_{sep,gas}} \times 100\%
\end{equation}
工程安全要求$\mathrm{HTO}_{\mathrm{pct}} \le 2\%$。定义系统常数：
\begin{equation}
  K_{HTO} = \frac{R}{P_{sys} \cdot V_{sep,gas}}
\end{equation}
为便于李导数推导，以单槽的7维状态向量为分析对象：
\begin{equation}
  \mathbf{x} = [x_1, \dots, x_7]^\top = [T_{s,in}, T_s, T_{sep}, T_{c,out}, n_{H_2,an}, n_{liq}, n_{gas}]^\top
\end{equation}
则HTO约束可写为状态空间形式$K_{HTO} \cdot x_3 \cdot x_7 \le 0.02$，其中$x_3 = T_{sep}$，$x_7 = n_{gas}$。

选取CBF候选函数如式~\eqref{eq:h_hto} 所示，将HTO安全约束显式写为系统状态的函数：
\begin{equation}
  h_{HTO}(\mathbf{x}) = 0.02 - K_{HTO} \cdot x_3 \cdot x_7
\label{eq:h_hto}
\end{equation}
上述CBF函数的梯度向量如下：
\begin{equation}
  \nabla h_{HTO} = \left[ 0,\ 0,\ -K_{HTO} \cdot x_7,\ 0,\ 0,\ 0,\ -K_{HTO} \cdot x_3 \right]
\end{equation}

CBF的时间导数由链式法则给出：
\begin{equation}
  \dot{h}_{HTO} = \nabla h_{HTO} \cdot f(\mathbf{x}, \mathbf{u}) = -K_{HTO} \cdot \left( x_7 \dot{x}_3 + x_3 \dot{x}_7 \right)
\label{eq:h_hto_dot_chain}
\end{equation}

分离器温度动态$\dot{x}_3$为：
\begin{equation}
  \dot{x}_3 = \frac{1}{C_{sep}}\left[ c_{lye}\rho_{lye} u_2 (x_2 - x_3) - Q_{sep,diss}(x_3) \right]
\end{equation}
其中$Q_{sep,diss}(x_3) = h_{sep}(x_3 - T_{amb})$为分离器热耗散功率。

气相氢气动态$\dot{x}_7$为：
\begin{equation}
  \dot{x}_7 = \frac{x_6}{\tau_{sep}} - \frac{R \cdot x_3 \cdot x_7 \cdot N_{cell} \cdot u_1 \cdot \eta_F(u_1, x_2)}{4F \cdot P_{sys} \cdot V_{sep,gas}}
\end{equation}
其中关键的非仿射耦合项为$u_1 \cdot \eta_F(u_1, x_2)$，法拉第效率：
\begin{equation}
  \eta_F(I, T_s) = \frac{I^2}{f_1(T_s) + I^2} \cdot f_2(T_s)
\end{equation}
温度依赖参数定义如下：
\begin{align}
  f_1(T_s) &= 50 + 2.5(T_s - 273.15) \\
  f_2(T_s) &= 0.92 - 6.25 \times 10^{-6}(T_s - 273.15)
\end{align}

将$\dot{x}_3$与$\dot{x}_7$代入式~\eqref{eq:h_hto_dot_chain} 后整理，可得控制仿射与非仿射混合形式：
\begin{equation}
  \dot{h}_{HTO}(\mathbf{x}, \mathbf{u}) = \underbrace{\gamma(\mathbf{x}) \cdot u_1 \cdot \eta_F(u_1, x_2)}_{\text{电流控制（非仿射）}} - \underbrace{\beta(\mathbf{x}) \cdot u_2}_{\text{流量控制（仿射）}} + \underbrace{\phi(\mathbf{x})}_{\text{纯状态漂移}}
\label{eq:h_hto_expanded}
\end{equation}

其中各系数定义如下：
\begin{align}
  \gamma(\mathbf{x}) &= \frac{R^2 \cdot N_{cell} \cdot x_3^2 \cdot x_7}{4F \cdot P_{sys}^2 \cdot V_{sep,gas}^2} > 0 \quad (\text{当 } x_3, x_7 > 0) \\
  \beta(\mathbf{x}) &= \frac{K_{HTO} \cdot c_{lye} \cdot \rho_{lye} \cdot x_7 \cdot (x_2 - x_3)}{C_{sep}} \\
  \phi(\mathbf{x}) &= \frac{K_{HTO} \cdot x_7 \cdot Q_{sep,diss}(x_3)}{C_{sep}} - \frac{K_{HTO} \cdot x_3 \cdot x_6}{\tau_{sep}}
\end{align}

\subsubsection{非仿射CBF优化问题}

在每个控制时刻，CBF安全滤波器求解以下点wise优化问题：
\begin{equation}
  \begin{aligned}
    \min_{\mathbf{u}\in\mathcal{U}} \quad & \|\mathbf{u} - \mathbf{u}_{ref}\|_2^2 \\
    \text{s.t.} \quad & \gamma(\mathbf{x}) \cdot u_1 \cdot \eta_F(u_1, x_2) - \beta(\mathbf{x}) \cdot u_2 + \phi(\mathbf{x}) + \gamma_{CBF} h_{HTO}(\mathbf{x}) \ge 0 \\
    & \mathbf{u}_{min} \le \mathbf{u} \le \mathbf{u}_{max}
  \end{aligned}
\label{eq:cbf_optimization}
\end{equation}

由于式~\eqref{eq:cbf_optimization} 的约束包含$u_1 \cdot \eta_F(u_1, x_2)$这一关于电流的非仿射项，标准的二次规划（QP）方法不再适用。
借助CasADi/IPOPT求解该非凸非线性优化问题，通过符号自动微分精确计算李导数，确保CBF条件的严格满足。
CBF安全滤波器的执行流程如算法~\ref{algo:cbf_filter} 所示。

\begin{algorithm}[htb]
  \caption{非仿射CBF安全滤波}
  \label{algo:cbf_filter}
  \SetAlgoLined
  \KwIn{当前状态$\mathbf{x}_t$，参考控制量$\mathbf{u}_{ref}$，CBF参数$\{\gamma_i\}_{i=1}^5$}
  \KwOut{安全控制量$\mathbf{u}_t$}
  计算各CBF函数值$\{h_i(\mathbf{x}_t)\}_{i=1}^5$\\
  利用符号自动微分计算李导数$\{\dot{h}_i(\mathbf{x}_t, \mathbf{u})\}_{i=1}^5$\\
  构建联合CBF约束：$\dot{h}_i(\mathbf{x}_t, \mathbf{u}) + \gamma_i h_i(\mathbf{x}_t) \ge 0,\quad i=1,\dots,5$\\
  求解带约束优化问题：\\
  \quad$\mathbf{u}_t = \arg\min_{\mathbf{u}_{min} \le \mathbf{u} \le \mathbf{u}_{max}} \|\mathbf{u} - \mathbf{u}_{ref}\|_2^2$\\
  \qquad s.t. $\dot{h}_i(\mathbf{x}_t, \mathbf{u}) + \gamma_i h_i(\mathbf{x}_t) \ge 0,\quad \forall i$\\
  \If{优化问题不可行}{
    触发可行性回退：$\mathbf{u}_t \leftarrow \mathbf{u}_{min}$ 或采用最近可行解\\
  }
  \Return{$\mathbf{u}_t$}
\end{algorithm}

\subsubsection{温度约束的CBF推导}

除HTO约束外，温度上下限同样需要严格满足。选取仅依赖于状态$x_2 = T_s$的CBF候选函数：
\begin{equation}
  \begin{aligned}
    h_1(\mathbf{x}) &= 363.15 - x_2, \\
    h_2(\mathbf{x}) &= x_2 - 293.15
  \end{aligned}
  \label{eq:h_temperature}
\end{equation}
其中$h_1$对应温度上限（$90^\circ$C），$h_2$对应温度下限（$20^\circ$C）。

$h_1$的梯度向量仅对$x_2$有非零偏导：
\begin{equation}
  \nabla h_1 = \left[ 0,\ -1,\ 0,\ 0,\ 0,\ 0,\ 0 \right]
\end{equation}

根据链式法则，其时间导数为：
\begin{equation}
  \dot{h}_1 = \nabla h_1 \cdot f(\mathbf{x}, \mathbf{u}) = -\dot{x}_2 = -\dot{T}_s
\end{equation}

将电解槽热平衡动态$\dot{x}_2$代入并展开整理，可得控制仿射与非仿射混合形式：
\begin{equation}
  \dot{h}_1(\mathbf{x}, \mathbf{u}) = \underbrace{\alpha(\mathbf{x}, u_1)}_{\text{电流控制（非仿射）}} + \underbrace{\beta(\mathbf{x}) \cdot u_2}_{\text{流量控制（仿射）}} + \underbrace{\psi(\mathbf{x})}_{\text{纯状态漂移}}
  \label{eq:h1_dot}
\end{equation}

其中各系数定义如下：
\begin{align}
  \alpha(\mathbf{x}, u_1) &= -\frac{N_{cell}}{C_s} u_1 \left[ U_{cell}(u_1, x_2) - \eta_F(u_1, x_2) U_{th} \right] \\
  \beta(\mathbf{x}) &= \frac{c_{lye} \cdot \rho_{lye} \cdot (x_2 - x_1)}{C_s} \\
  \psi(\mathbf{x}) &= \frac{\sigma_s(x_2 - T_{am}) + \epsilon_s \sigma_b A_{stack}(x_2^4 - T_{am}^4)}{C_s}
\end{align}

$\alpha(\mathbf{x}, u_1)$关于电流$u_1$呈非仿射关系，包含$u_1$与电压$U_{cell}$以及法拉第效率$\eta_F$的耦合；
$\beta(\mathbf{x})$为碱液流量$u_2$的仿射系数，其符号由$x_2 - x_1$决定；
$\psi(\mathbf{x})$仅依赖于状态$x_2 = T_s$，反映对流与辐射散热的被动影响，与控制输入无关。
由于$U_{cell} > \eta_F U_{th}$，产热功率恒为正，故$\alpha(\mathbf{x}, u_1) < 0$，意味着增大电流会使$\dot{h}_1$减小（温度上升，安全裕度降低）。
当$x_2 > x_1$时，$\beta(\mathbf{x}) > 0$，增大$u_2$可使$\dot{h}_1$增大（带走更多热量，提升安全裕度）。

类似地，$h_2$的梯度为$\nabla h_2 = [0, 1, 0, 0, 0, 0, 0]$，时间导数为：
\begin{equation}
  \dot{h}_2(\mathbf{x}, \mathbf{u}) = \underbrace{-\alpha(\mathbf{x}, u_1)}_{\text{电流控制（非仿射）}} - \underbrace{\beta(\mathbf{x}) \cdot u_2}_{\text{流量控制（仿射）}} - \underbrace{\psi(\mathbf{x})}_{\text{纯状态漂移}} = -\dot{h}_1(\mathbf{x}, \mathbf{u})
  \label{eq:h2_dot}
\end{equation}

温度约束的CBF条件分别为：
\begin{equation}
  \begin{aligned}
    \dot{h}_1(\mathbf{x}, \mathbf{u}) + \gamma_1 h_1(\mathbf{x}) &\ge 0, \\
    \dot{h}_2(\mathbf{x}, \mathbf{u}) + \gamma_2 h_2(\mathbf{x}) &\ge 0
  \end{aligned}
  \label{eq:cbf_temperature}
\end{equation}

\subsubsection{多重CBF联合约束}

系统还需同时满足电压上限与功率上限约束。为此，定义如下多重CBF：
\begin{equation}
  \begin{array}{ll}
    h_1(\mathbf{x}) = 363.15 - x_2, & \text{（温度上限，90$^\circ$C）} \\
    h_2(\mathbf{x}) = x_2 - 293.15, & \text{（温度下限，20$^\circ$C）} \\
    h_3(\mathbf{x}) = 2.2 - U_{cell}(x_2, u_1), & \text{（电压上限）} \\
    h_4(\mathbf{x}) = 6\times10^6 - P(x_2, u_1), & \text{（功率上限）} \\
    h_5(\mathbf{x}) = 0.02 - K_{HTO} \cdot x_3 \cdot x_7, & \text{（HTO上限）}
  \end{array}
\end{equation}
联合CBF条件要求同时满足：
\begin{equation}
  \dot{h}_i(\mathbf{x}, \mathbf{u}) + \gamma_i h_i(\mathbf{x}) \ge 0, \quad \forall i = 1, \dots, 5
\label{eq:joint_cbf}
\end{equation}
在优化求解时，式~\eqref{eq:joint_cbf} 作为多个不等式约束同时处理。

\subsubsection{物理意义分析}

由式~\eqref{eq:h_hto_expanded} 可分析各控制动作对$\dot{h}_{HTO}$的影响：
\begin{itemize}
  \item \textbf{增大电流$u_1$}：由于$\gamma(\mathbf{x})>0$且$\eta_F>0$，增大$u_1$会正向提升$\dot{h}_{HTO}$，即推动系统远离HTO边界；
  \item \textbf{增大碱液流速$u_2$}：其影响取决于$\beta(\mathbf{x})$的符号。若$x_2 > x_3$，增大$u_2$可降低分离器温度，从而间接降低HTO；
  \item \textbf{漂移项$\phi(\mathbf{x})$}：反映散热与液相传质的被动影响。
\end{itemize}

综上，生成模型控制策略可分别与安全投影算子或CBF安全滤波结合，形成两种并行的安全预测控制闭环，其执行流程如算法~\ref{algo:safe_closed_loop_control} 所示。

\begin{algorithm}[htb]
  \caption{基于扩散模型与安全约束的闭环控制}
  \label{algo:safe_closed_loop_control}
  \SetAlgoLined
  \KwIn{初始状态$\mathbf{x}_0$，功率参考轨迹$\{P_{ref,t}\}$，策略模型$\pi_\theta$，安全机制$\mathcal{S}$（安全投影$\mathcal{P}$或CBF滤波$\mathcal{F}_{CBF}$）}
  \KwOut{闭环状态轨迹$\{\mathbf{x}_t\}$与控制轨迹$\{\mathbf{u}_t\}$}
  \For{每个控制周期$t = 0, 1, 2, \ldots$}{
    采样$K$条候选动作序列$\{\mathbf{a}^{(i)}\}_{i=1}^K \sim \pi_\theta(\mathbf{x}_t, P_{ref,t:t+N_p-1})$\\
    \For{$i = 1, \ldots, K$}{
      使用物理模型推演轨迹并计算代价$J^{(i)}$\\
    }
    按选择概率$p^{(i)} \propto \exp(-J^{(i)}/\alpha)$采样名义动作$\mathbf{u}_{nom}$\\
    $\mathbf{u}_t \leftarrow \mathcal{S}(\mathbf{u}_{nom}, \mathbf{x}_t)$ \tcp*{根据配置选择安全投影或CBF滤波}
    将$\mathbf{u}_t$施加到多时间尺度仿真器，推进至下一状态$\mathbf{x}_{t+1}$\\
  }
  \Return{$\{\mathbf{x}_t\}, \{\mathbf{u}_t\}$}
\end{algorithm}

下文将通过闭环仿真实验，验证安全投影算子与CBF约束保证机制在四槽AWE系统中的实际控制效果。

\section{实验验证与结果分析}

\subsection{闭环仿真结果}

\subsubsection{功率跟踪性能}

表~\ref{tab:performance_comparison} 对比了不同控制方法在四槽系统上的性能。

\begin{table}[!htbp]
  \centering
  \caption{控制方法性能对比（四槽系统）}
  \label{tab:performance_comparison}
  \begin{tabular}{@{}l c c c@{}}
    \toprule
    \textbf{方法} & \textbf{功率RMSE [MW]} & \textbf{温度MAE [K]} & \textbf{推理时间 [ms]} \\
    \midrule
    NMPC & 0.12 & 1.6 & 2000--5000 \\
    Diffusion TCN & 0.14 & 2.0 & 50 \\
    Diffusion TCN + 安全投影 & 0.14 & 2.0 & $\sim$50 \\
    \bottomrule
  \end{tabular}
\end{table}

图~\ref{fig:multi_stack_comparison} 展示了NMPC、Flow TCN与Diffusion TCN在1小时闭环仿真中的跟踪与控制效果。
由图可见，三种方法均能较好地跟踪参考功率曲线，但NMPC的功率跟踪误差最小，这与其在线优化求解的本质一致。
Flow TCN与Diffusion TCN的功率跟踪精度接近，略高于NMPC。
在温度调节方面，三种方法的温度波动均在安全范围内，但Diffusion TCN的温度跟踪误差略大于NMPC。
HTO指标方面，所有方法均能将气相氢气浓度控制在2\%安全阈值以下。
值得注意的是，NMPC的单次控制计算时间高达2000--5000 ms，而Diffusion TCN和Flow TCN仅需约50 ms，实现了近两个数量级的加速，显著满足了实时控制需求。

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.98\textwidth]{figures/multi_stack_comparison.png}
  \caption{NMPC、Flow TCN与Diffusion TCN闭环控制效果对比}
  \label{fig:multi_stack_comparison}
\end{figure}

\subsubsection{安全性分析}

在引入投影算子和CBF的安全约束保证机制后，系统在全部测试时段内严格满足各项安全约束：
\begin{itemize}
  \item \textbf{HTO约束}：气相氢气浓度始终低于2\%的安全阈值，无越界情况；
  \item \textbf{温度约束}：各电解槽温度均保持在293.15--363.15 K（20--90°C）范围内，满足率100\%；
  \item \textbf{电压与功率约束}：单槽电压不超过2.2 V，单槽功率不超过6 MW，约束满足率100\%。
\end{itemize}

图~\ref{fig:multi_stack_cbf_unified} 展示了多重CBF联合约束在低功率HTO工况下的闭环验证结果。
图中从上至下依次为四槽电流、碱液流量、总功率、HTO百分比、电解槽温度、平均电压，以及各CBF约束的原始值$h$、李导数$L_f h$和CBF条件$\dot{h}+\gamma h$的演化曲线。
可以看到，尽管参考电流较低（约600 A），但在前30分钟内各CBF条件均保持在零以上，未出现约束违反。
CBF投影的求解时间（右下角）平均值低于15 ms，验证了该安全机制的计算可行性。

\begin{figure}[p]
  \centering
  \includegraphics[width=0.98\textwidth]{figures/multi_stack_cbf_unified.png}
  \caption{多重CBF联合约束闭环验证结果（低功率HTO工况）}
  \label{fig:multi_stack_cbf_unified}
\end{figure}

上述结果表明，所提出的安全增强机制在不显著牺牲跟踪性能的前提下
严格保证系统的安全运行。

为进一步验证CBF在高温极端工况下的约束保证能力，
设计了高功率产热测试场景：将四槽参考电流均设为8000~A，冷却水流量设为0~m$^3$/s，
人为制造强烈的温升趋势。
在此条件下，若不加安全约束，电解槽温度将迅速突破90$^\circ$C上限。
图~\ref{fig:multi_stack_cbf_high_temp} 展示了该工况下的多槽CBF闭环验证结果。
由图可见，CBF安全滤波通过主动增加冷却水流量并适度降低电流，
将各槽温度严格控制在90$^\circ$C以下（最高温度为89.87$^\circ$C），
HTO指标亦保持在2\%安全阈值以内，求解时间仍在20~ms以内。

\begin{figure}[p]
  \centering
  \includegraphics[width=0.98\textwidth]{figures/multi_stack_cbf_high_temp.png}
  \caption{多重CBF联合约束闭环验证结果（高温极端工况）}
  \label{fig:multi_stack_cbf_high_temp}
\end{figure}

为验证CBF在功率上限约束下的有效性，设计了高功率测试场景：四槽参考电流均设为9360~A（接近电流上限），冷却水流量设为0.5~m$^3$/s，
碱液流量设为0.03~m$^3$/s，以主动触发功率上限（6~MW/槽）和电压上限（2.2~V）约束。
较高的冷却水流量参考用于防止温度约束被触发，使CBF专注于功率和电压约束的处理。
图~\ref{fig:multi_stack_cbf_high_power} 展示了该工况下的多槽CBF闭环验证结果。
CBF安全滤波通过主动降低电流（约从9360~A降至8722~A），将各槽功率严格控制在6~MW以下，
电压保持在2.2~V以内，最高温度为84.90$^\circ$C，HTO指标亦保持在2\%安全阈值以内，
求解时间平均值约23~ms，仍在实时控制要求范围内。

进一步在单槽场景下验证功率约束主导的CBF行为：参考电流设为9360~A，冷却水流量设为1.0~m$^3$/s，
采用硬温度约束以确保温度安全。结果显示CBF将电流从9360~A降至约9014~A，
主要受功率和电压约束限制，最高温度为89.50$^\circ$C，
HTO指标保持在0.928\%，验证了功率约束主导的CBF修正策略。

\begin{figure}[p]
  \centering
  \includegraphics[width=0.98\textwidth]{figures/multi_stack_cbf_high_power.png}
  \caption{多重CBF联合约束闭环验证结果（高功率工况）}
  \label{fig:multi_stack_cbf_high_power}
\end{figure}

\subsubsection{安全投影对比实验}

安全投影算子与CBF安全滤波是两种并行的安全约束保证机制。
前者基于单步预测将控制量修正到可行集内，后者通过李导数构建前向不变性理论保证。
为对比两种机制的效果，设计了三组实验：
（1）无安全机制的纯Diffusion TCN（Basic）；
（2）Diffusion TCN + 安全投影算子（Safe）；
（3）Diffusion TCN + CBF安全滤波（CBF）。
实验在同一组风电功率曲线下进行闭环仿真，对比结果如表~\ref{tab:safety_projection_comparison} 所示。

\begin{table}[!htbp]
  \centering
  \caption{安全投影对比实验结果}
  \label{tab:safety_projection_comparison}
  \begin{tabular}{@{}l c c c c@{}}
    \toprule
    \textbf{配置} & \textbf{功率RMSE [MW]} & \textbf{温度MAE [K]} & \textbf{约束违反次数} & \textbf{单次推理时间 [ms]} \\
    \midrule
    Diffusion TCN（无安全） & 0.14 & 2.0 & 12 & 50 \\
    \addlinespace
    Diffusion TCN + 安全投影 & 0.14 & 2.0 & 0 & 58 \\
    \addlinespace
    Diffusion TCN + CBF滤波 & 0.14 & 2.0 & 0 & 72 \\
    \bottomrule
  \end{tabular}
\end{table}

从表~\ref{tab:safety_projection_comparison} 可得出以下结论：
\begin{itemize}
  \item \textbf{无安全机制时}，纯Diffusion TCN在动态跟踪过程中出现了12次约束违反，主要集中在功率骤升导致温度接近上限或HTO短时超界的极端工况。这验证了纯数据驱动方法虽然整体精度较高，但无法对安全约束提供严格保证。

  \item \textbf{采用安全投影后}，所有约束违反均被消除。投影算子通过单步预测将不安全的控制量修正到可行集内，对跟踪性能几乎无影响（功率RMSE和温度MAE保持不变），单次投影求解平均耗时约8 ms，总推理时间从50 ms增至58 ms。

  \item \textbf{采用CBF安全滤波后}，系统在理论层面获得了前向不变性保证。CBF优化平均耗时约14 ms，总推理时间约为72 ms，仍在100 ms的实时控制要求以内。
\end{itemize}

图~\ref{fig:multi_stack_cbf_comparison} 进一步展示了低功率HTO风险工况下，三种配置（Basic、Safe、CBF）的闭环对比结果。
在该工况中，参考功率接近于零，气相氢气容易累积导致HTO上升。
由图可见，无安全机制的Basic控制器（蓝色曲线）的HTO持续升高并超出2\%安全阈值，最终在第25分钟左右发生严重越界；
采用安全投影后（绿色曲线），HTO被有效抑制在安全边界以下，电流和碱液流量均在约束范围内；
采用CBF安全滤波后（红色曲线），系统在早期即主动调整电流策略——通过适当提高电流促进氧气产率，从而"稀释"气相氢气，使HTO始终保持在更低水平。

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.98\textwidth]{figures/multi_stack_cbf_comparison.png}
  \caption{低功率HTO工况下安全投影与CBF的闭环对比}
  \label{fig:multi_stack_cbf_comparison}
\end{figure}

\subsection{消融实验}

\subsubsection{模型架构对比}

为验证网络架构对控制性能的影响，对比了TCN与MLP两种主干网络。
TCN利用空洞卷积构建指数级增长的有效感受野，更好地捕捉动作序列中的长程时序依赖。
而MLP虽然结构简单，但缺乏显式的时间维度建模能力。
对比结果显示，在相同的隐藏层维度与训练轮数下，TCN的功率跟踪RMSE比MLP降低约15\%，
且生成的动作序列平滑性更优，电流切换幅度更小，有利于延长执行机构寿命。
综合考虑性能与模型复杂度，故选用TCN作为扩散策略的主干网络。

\subsubsection{采样数量影响}

扩散模型推理时需要从噪声中采样多条候选动作序列，采样数量直接影响动作选择的多样性与计算开销。
进一步分析发现，当采样数从16增加到64时，样本量的增加覆盖了多样的动作模式，功率跟踪误差显著下降。
当采样数继续增加到128乃至256时，性能提升趋于饱和，而推理时间近似线性增长。
因此，64--128个样本是四槽AWE控制任务中性能与计算效率的较优平衡点，
后续实验均采用64个样本评估候选动作。

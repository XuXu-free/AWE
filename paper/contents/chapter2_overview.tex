% !TeX root = ../main.tex

\chapter{AWE系统建模与仿真}
\label{chap:modeling}

本章建立四槽并联碱性电解水（AWE）系统的完整动力学模型，为后续控制策略设计与仿真验证提供物理基础。
首先介绍多槽并联系统的整体架构与基本假设；随后从单槽出发，依次建立电化学模型、热力学模型和氢气传输模型；在此基础上引入多槽共用换热器和分离器带来的耦合机制，扩展得到多槽耦合系统的统一状态空间模型；最后搭建涵盖物理仿真、数据生成与闭环验证的完整实验平台。

\section{系统整体架构}

\subsection{多槽并联AWE系统结构}

所研究的碱性电解水（AWE）系统由$N$个电解槽并联组成，各槽共享碱液循环回路、冷却回路与气液分离单元，但分别由独立的功率变换单元调节电流。
以四槽系统（$N=4$）为例，其结构如图~\ref{fig:system_architecture} 所示。
系统主要由$N$个并联的电解槽及其功率变换单元、碱液循环和冷却回路，以及氢侧/氧侧气液分离单元组成。

% system_structure.png 系统架构图
\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.95\textwidth]{figures/system_structure.png}
  \caption{四槽并联AWE系统结构示意图}
  \label{fig:system_architecture}
\end{figure}

图~\ref{fig:system_architecture} 展示了系统的主要物理组成模块：
\begin{itemize}
  \item \textbf{逆变器（功率变换单元）}：每个电解槽配备独立的AC/DC逆变器，将电网交流电与可再生能源转换为电解槽系统可用的直流电，并提供电流的调节与控制功能
  \item \textbf{电解槽}：由多个电芯串联组成，在直流电作用下将水分解为氢气和氧气，同时产生废热
  \item \textbf{气液分离器}：将电解槽出口的氢-碱液和氧-碱液混合物分别送入氢侧与氧侧气液分离器进行气液分离，维持系统压力；其中氢侧气液分离器出口配置HTO检测装置，用于监测气体纯度
  \item \textbf{碱液泵}：驱动碱液在电解槽与换热器之间循环流动，通过调节转速控制碱液流量，从而维持电解反应所需的电解液供给并辅助温度调节
  \item \textbf{冷凝水换热器}：通过调节冷却水流量控制换热强度，带走碱液中的多余热量，进而决定碱液温度与电解槽运行温度
  \item \textbf{控制器}：根据上层功率调度指令与实时状态反馈，计算并下发各电解槽电流、碱液流量和冷却水流量的设定值
\end{itemize}

\subsection{基本假设}

在建模之前，首先明确以下基本假设，以平衡模型精度与计算复杂度：

\begin{enumerate}
  \item 同一电解槽内所有电解电池在相同电流和电压下运行；
  \item 秒级内稳定的动态变化过程（如气泡体积变化和液位波动）被忽略；
  \item 电解槽建模为零维系统，假设温度、压力和气体在槽内均匀分布；
  \item 流出碱液的温度等于电解槽温度；
  \item 碱液温度仅受电解反应和换热器影响，循环过程中的温度变化被忽略；
  \item 离开分离器的氢气和氧气流量等于各自的生产速率。
\end{enumerate}

\section{单槽AWE系统建模}

单槽AWE系统是构成多槽系统的基本单元。本节建立单槽系统的电化学、热力学和氢气传输模型，为后续多槽耦合建模奠定基础。
本章所涉及的模型参数与控制器参数取值统一汇总于附录~\ref{app:params}。

\subsection{单槽电化学模型}

\subsubsection{电解槽电压模型}

单电芯电压由可逆电压、欧姆过电位和活化过电位三部分组成，分别对应电解反应的热力学平衡电势、电解液与隔膜电阻的欧姆损耗以及电极表面电化学反应的动力学损耗：
\begin{equation}
  U_{cell} = U_{rev} + U_{ohm} + U_{act}
\end{equation}

其中，可逆电压$U_{rev}$由能斯特方程确定，反映了给定工况下电解反应的热力学极限电势。欧姆过电位描述电流流经电解液和隔膜时的电阻压降：
\begin{equation}
  U_{ohm} = R_{ohm} \cdot I = (r_1 + r_2 \cdot T_s + r_3 \cdot P_{sys}) \cdot I
\end{equation}

其中$r_1,r_2,r_3$为欧姆过电位模型参数，$P_{sys}$为系统压力。

活化过电位采用对数形式：
\begin{equation}
  U_{act} = s \cdot \ln(t_{act} \cdot I + 1)
\end{equation}

其中$t_{act} = t_1 + t_2/T_C + t_3/T_C^2$，$T_C = T_s - 273.15$为摄氏温度。

\subsubsection{法拉第效率模型}

法拉第效率描述实际产氢量与理论产氢量之比，受寄生反应（如氢氧复合反应）影响。本文采用如下有理函数形式描述其随电流和温度的变化规律：
\begin{equation}
  \eta_F = \frac{I^2}{f_1 + I^2} \cdot f_2
\end{equation}

其中，$f_1$决定效率曲线的拐点位置，$f_2$为极限效率。参数定义如下：
\begin{align}
  f_1 &= 50 + 2.5 \cdot T_C \\
  f_2 &= 0.92 - 6.25 \times 10^{-6} \cdot T_C
\end{align}

该模型反映了低电流区法拉第效率较低（寄生反应占主导），高电流区趋近于极限值$f_2$的特性。

\subsubsection{产热功率与氢气产率}

电解槽产热功率可表示为：
\begin{equation}
  Q_{ele} = N_{cell} \cdot I \cdot (U_{cell} - \eta_F \cdot U_{th})
\end{equation}

其中$U_{th}$为热中性电压，$N_{cell}$为单槽电芯数。

氢气产生速率满足以下关系：
\begin{equation}
  \dot{n}_{H_2,prod} = \frac{\eta_F \cdot I \cdot N_{cell}}{2F}
\end{equation}

氧气产生速率（化学计量比2:1）：
\begin{equation}
  \dot{n}_{O_2,prod} = \frac{\eta_F \cdot I \cdot N_{cell}}{4F}
\end{equation}

其中$F$为法拉第常数。

\subsection{单槽热力学模型}

\subsubsection{电解槽热平衡}

单个电解槽的温度动态由热平衡方程描述。电解槽内部产热、表面散热和碱液对流带走的热量共同决定温度变化速率：
\begin{equation}
  C_s \frac{dT_s}{dt} = Q_{ele} - Q_{diss} - Q_{flow}
\end{equation}

其中，$C_s$为电解槽等效热容，$Q_{ele}$为电化学反应产热功率，$Q_{diss}$为表面散热功率，$Q_{flow}$为碱液对流带走的热量。表面散热$Q_{diss}$包括对流和辐射两部分：
\begin{equation}
  Q_{diss} = Q_{conv} + Q_{rad}
\end{equation}
其中
\begin{align}
  Q_{conv} &= \sigma_s \cdot (T_s - T_{am}) \\
  Q_{rad} &= \epsilon_s \cdot \sigma_b \cdot A_{stack} \cdot (T_s^4 - T_{am}^4)
\end{align}

碱液对流带走的热量：
\begin{equation}
  Q_{flow} = c_{lye} \cdot \rho_{lye} \cdot v_{lye} \cdot (T_s - T_{s,in})
\end{equation}

其中$c_{lye}$为碱液比热容，$\rho_{lye}$为碱液密度。

\subsubsection{换热器与冷却系统}

换热器负责将电解槽出口高温碱液的热量传递给冷却水，维持系统热平衡。其传热计算基于对数平均温差（LMTD）方法，适用于逆流或并流换热器的稳态传热分析：
\begin{equation}
  Q_{hx} = k_{he} \cdot A_{he} \cdot LMTD
\end{equation}

其中，$k_{he}$为总传热系数，$A_{he}$为传热面积。对数平均温差定义为：
\begin{equation}
  LMTD = \begin{cases}
    0, & (T_{s,in} - T_{c,out})(T_{sep} - T_{c,in}) \leq 0 \\[6pt]
    \dfrac{(T_{s,in} - T_{c,out}) - (T_{sep} - T_{c,in})}{\ln\left(\dfrac{T_{s,in} - T_{c,out}}{T_{sep} - T_{c,in}}\right)}, & \text{其他}
  \end{cases}
\end{equation}

换热器碱液出口温度动态：
\begin{equation}
  C_{he} \frac{dT_{s,in}}{dt} = c_{lye} \cdot \rho_{lye} \cdot v_{lye} \cdot (T_{sep} - T_{s,in}) - Q_{hx}
\end{equation}

冷却水出口温度动态：
\begin{equation}
  C_c \frac{dT_{c,out}}{dt} = c_{cw} \cdot \rho_{cw} \cdot v_c \cdot (T_{c,in} - T_{c,out}) + Q_{hx}
\end{equation}

\subsubsection{分离器热力学}

分离器温度动态方程为：
\begin{equation}
  C_{sep} \frac{dT_{sep}}{dt} = 0.5 \cdot c_{lye} \cdot \rho_{lye} \cdot v_{lye} \cdot (T_{s} - T_{sep}) - Q_{sep,diss}
\end{equation}

其中分离器热耗散$Q_{sep,diss}$包括对流和辐射两部分：
\begin{equation}
  Q_{sep,diss} = Q_{sep,conv} + Q_{sep,rad}
\end{equation}
其中
\begin{align}
  Q_{sep,conv} &= \sigma_{sep} \cdot (T_{sep} - T_{am}) \\
  Q_{sep,rad} &= \epsilon_{sep} \cdot \sigma_b \cdot A_{sep} \cdot (T_{sep}^4 - T_{am}^4)
\end{align}

\subsection{单槽氢气传输与HTO模型}

单槽AWE系统的氢气传输与HTO（Hydrogen-to-Oxygen）杂质模型描述了氢气从阴极产生、跨膜传输至阳极、随碱液进入分离器，最终在气液两相中分布的完整物理过程，如图~\ref{fig:single_stack_hto}所示。

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.95\textwidth]{figures/hto.png}
  \caption{单槽氢气传输与HTO模型示意图}
  \label{fig:single_stack_hto}
\end{figure}

\subsubsection{氢气溶解度}

氢气在碱液中的溶解度采用Secchenov方程结合Henry定律计算：
\begin{equation}
  S_{H_2,lye} = \frac{S_{H_2,H_2O}}{10^{K_{H_2} \cdot w_{lye}}}
\end{equation}

其中氢气在纯水中的溶解度：
\begin{equation}
  S_{H_2,H_2O} = \frac{\rho_{H_2O} \cdot P_{sys}}{M_{H_2O} \cdot p_{atm} \cdot H_{H_2}}
\end{equation}

\subsubsection{阳极室氢气动态}

阳极室溶解氢气摩尔数$n_{H_2,an}$的动态由三部分输入和一部分输出组成：

\textbf{输入1：碱液循环带入}
\begin{equation}
  \dot{n}_{H_2,lye} = \frac{S_{H_2,lye} \cdot \rho_{lye} \cdot v_{lye}}{4}
\end{equation}

\textbf{输入2：跨膜扩散（菲克定律）}
\begin{equation}
  \dot{n}_{H_2,diff} = \frac{A_{cell} \cdot N_{cell} \cdot D_{eff} \cdot S_{H_2,lye} \cdot P_{sys}}{\delta}
\end{equation}

\textbf{输入3：压力差对流（达西定律）}
\begin{equation}
  \dot{n}_{H_2,conv} = A_{cell} \cdot N_{cell} \cdot \frac{K_{eff}}{\mu_{lye}} \cdot S_{H_2,lye} \cdot \rho_{lye} \cdot \frac{\Delta P}{\delta}
\end{equation}

总氢气输入速率可写为：
\begin{equation}
  \dot{n}_{H_2,im} = \dot{n}_{H_2,lye} + \dot{n}_{H_2,diff} + \dot{n}_{H_2,conv}
\end{equation}

\textbf{输出：向分离器传输}

假设阳极室内充分混合，氢气向分离器的传输速率：
\begin{equation}
  \dot{n}_{H_2,out} = \frac{n_{H_2,an} \cdot v_{lye}}{2 \cdot V_{an}}
\end{equation}

其中$V_{an}$为阳极室碱液体积。

阳极室氢气动态方程：
\begin{equation}
  \frac{dn_{H_2,an}}{dt} = \dot{n}_{H_2,im} - \dot{n}_{H_2,out}
\end{equation}

\subsubsection{分离器氢气动态}

分离器液相氢气摩尔数$n_{liq}$动态：
\begin{equation}
  \frac{dn_{liq}}{dt} = \dot{n}_{H_2,out} - \frac{n_{liq}}{\tau_{sep}}
\end{equation}

其中$\tau_{sep}$为气液分离时间常数。

分离器气相氢气摩尔数$n_{gas}$动态：
\begin{equation}
  \frac{dn_{gas}}{dt} = \frac{n_{liq}}{\tau_{sep}} - \dot{n}_{H_2,gas,out}
\end{equation}

氢气随氧气流流出速率：
\begin{equation}
  \dot{n}_{H_2,gas,out} = \frac{R \cdot T_{sep} \cdot n_{gas} \cdot \dot{n}_{O_2,prod}}{P_{sys} \cdot V_{sep,gas}}
\end{equation}

其中$V_{sep,gas}$为分离器气相体积，$V_{sep}$为分离器总体积。

\subsubsection{HTO计算}

氢气至氧侧传输量（HTO）定义为气相中氢气摩尔分数：
\begin{equation}
  HTO = \frac{n_{gas} \cdot R \cdot T_{sep}}{P_{sys} \cdot V_{sep,gas}} \times 100\%
\end{equation}

工程要求HTO严格低于上限$\mathrm{HTO}_{\mathrm{max}}$。

\subsection{单槽模型总结}

单槽系统的状态向量为7维：
\begin{equation}
  \mathbf{x}_{single} = [T_{s,in}, T_s, T_{sep}, T_{c,out}, n_{H_2,an}, n_{liq}, n_{gas}]^\top \in \mathbb{R}^7
\end{equation}

单槽系统的控制输入为3维：
\begin{equation}
  \mathbf{u}_{single} = [I, v_{lye}, v_c]^\top \in \mathbb{R}^3
\end{equation}

为深入理解单槽系统内部各变量之间的相互作用，以下对控制变量与状态变量之间的影响关系进行系统分析：

\textbf{电流} $I$：电流的调制同时影响效率、HTO和温度。由式~(2.4)和式~(2.5)可知，电流决定电解功率 $P_{ele}$ 和槽电压 $U_{cell}$；式~(2.10)进一步表明氢气产率 $\dot{n}_{H_2,prod}$ 和氧气产率 $\dot{n}_{O_2,prod}$ 均由电流决定，进而通过式~(2.15)和式~(2.28)分别改变HTO和温度。

\textbf{系统压力} $P_{sys}$：压力对效率和HTO具有显著影响。由式~(2.2)和式~(2.4b)可知，压力影响可逆电压 $U_{rev}$；此外，式~(2.19)表明压力显著影响氢气溶解度 $S_{H_2,lye}$，进而决定杂质摩尔流率 $\dot{n}_{H_2,im}$。

\textbf{碱液流量} $v_{lye}$：碱液流量主要影响温度和HTO。由式~(2.13)可知，碱液流量改变对流带走的热量 $Q_{flow}$；同时，式~(2.20)--(2.22)表明混合杂质摩尔流率 $\dot{n}_{H_2,im}$ 与碱液流量密切相关，从而显著影响HTO动态。

\textbf{冷却水流量} $v_c$：与上述参数不同，冷却水流量仅通过调节换热量 $Q_{hx}$ 影响冷却水温度 $T_{c,out}$ 和换热器出口碱液温度 $T_{s,in}$，进而间接影响电解槽温度，如式~(2.14)和式~(2.16)所示。

\textbf{状态变量间的动态耦合}：除控制变量对状态的影响外，状态变量之间也存在动态相互作用。具体而言，式~(2.12)表明电解功率 $P_{ele}$ 影响电解槽温度 $T_s$，而 $T_s$ 又通过式~(2.4)反作用于槽电压 $U_{cell}$，形成热电耦合回路；此外，式~(2.19)揭示温度显著影响氢气溶解度 $S_{H_2,lye}$，进而改变杂质传输速率与HTO水平，构成热-氢耦合通道。

上述变量耦合关系可归纳为：电流同时驱动电、热、氢三个子系统；压力和碱液流量是HTO的关键影响因素；冷却水流量仅参与热回路调节；而温度作为核心状态变量，反向影响电压和HTO，形成闭环耦合。明确这些耦合机理是后续设计协调控制策略的基础。

\section{多槽耦合系统建模}

将$N$个单槽通过共用换热器和气液分离器并联，构成多槽AWE系统。本节建立通用$N$槽耦合模型，实验验证取$N=4$。

由前述单槽建模可知，电化学模型（电压、法拉第效率、产热功率与氢气产率）、电解槽局部热平衡方程以及氢气溶解-跨膜传输模型对每个电解槽仍然独立成立，可直接复用。
然而，当多个电解槽并联后，以下环节发生本质变化：
\begin{itemize}
  \item \textbf{换热器共用}：碱液回路从单槽独立循环变为$N$槽汇流后共用一台换热器，总碱液流量为各槽流量之和，换热器出口温度$T_{s,in}$成为所有电解槽的共同输入；
  \item \textbf{分离器共用}：气液分离器由单槽独享变为$N$槽共用，分离器入口温度为各槽出口温度的流量加权平均，氢气与氧气汇入同一气相空间；
  \item \textbf{状态维度扩展}：状态向量从单槽的7维扩展为$2N+5$维，控制输入从3维扩展为$2N+1$维，以容纳各槽独立的电流与碱液流量；
  \item \textbf{HTO计算更新}：HTO定义形式不变，但氧气产生速率变为$N$个电解槽之和。
\end{itemize}

以下依次推导多槽共用设备带来的耦合热力学模型与氢气传输模型。

\subsection{多槽耦合热力学模型}

\subsubsection{换热器耦合模型}

$N$个电解槽共用换热器，总碱液流量：
\begin{equation}
  v_{tot} = \sum_{i=1}^{N} v_{lye,i}
\end{equation}

换热器动态与单槽形式相同，但使用总流量$v_{tot}$。

\subsubsection{分离器热力学耦合}

分离器入口温度为各槽出口温度的流量加权平均：
\begin{equation}
  T_{sep,in} = \frac{\sum_{i=1}^{N} v_{lye,i} \cdot T_{s,i}}{v_{tot}}
\end{equation}

分离器温度动态方程为：
\begin{equation}
  C_{sep} \frac{dT_{sep}}{dt} = 0.5 \cdot c_{lye} \cdot \rho_{lye} \cdot v_{tot} \cdot (T_{sep,in} - T_{sep}) - Q_{sep,diss}
\end{equation}

\subsection{多槽氢气传输与HTO耦合模型}

将$N$个单槽通过共用气液分离器并联后，氢气传输环节发生本质变化：各槽阳极室氢气动态仍独立成立，但分离器由单槽独享变为$N$槽共用，气相空间共享导致系统呈现显著的氢耦合特征。$N$槽并联系统的HTO耦合可从以下三个维度刻画：

\textbf{1. 分离器气相空间共享}。
单槽系统中，$n_{gas}$和$n_{liq}$为单个电解槽的局部状态；而在多槽系统中，分离器液相接收来自所有$N$个电解槽的氢气输入，气相空间为所有电解槽共用。任一分支阳极室氢气输入均会改变分离器内气液两相氢气分布，形成公共状态的耦合关联。

\textbf{2. 氧气产生速率的汇流效应}。
多槽系统的总氧气产生速率为各槽之和：
\begin{equation}
  \dot{n}_{O_2,tot} = \sum_{i=1}^{N} \dot{n}_{O_2,prod,i}
\end{equation}

分离器气相动态方程因此变为：
\begin{equation}
  \frac{dn_{gas}}{dt} = \frac{n_{liq}}{\tau_{sep}} - \frac{R \cdot T_{sep} \cdot n_{gas} \cdot \sum_{i=1}^{N} \dot{n}_{O_2,prod,i}}{P_{sys} \cdot V_{sep,gas}}
\end{equation}

其中第二项为氢气随氧气流流出速率，其分母中的总产氧速率放大稀释效应，但分子中的公共$n_{gas}$使HTO对所有分支的氢气输入敏感。

\textbf{3. HTO的系统级敏感性}。
多槽系统的HTO定义形式与单槽相同，但氧气产生速率为各槽之和：
\begin{equation}
  HTO = \frac{n_{gas} \cdot R \cdot T_{sep}}{P_{sys} \cdot V_{sep,gas}} \times 100\%
\end{equation}

由于$n_{gas}$为所有电解槽的公共状态，某一分支因电流异常或温度波动导致产氢增加时，氢气将汇入共用分离器，直接抬升系统整体HTO水平。因此，多槽系统的HTO安全约束具有全局性：任一分支的局部异常均可能触发系统级安全事件，这对协调控制策略的全局优化能力提出了更高要求。

\subsection{多槽建模总结}

基于上述多槽耦合特性，$N$槽系统的状态向量为$(2N+5)$维：
\begin{equation}
  \mathbf{x} = [T_{s,in}, T_{s,1}, \ldots, T_{s,N}, T_{sep}, T_{c,out}, n_{H_2,an,1}, \ldots, n_{H_2,an,N}, n_{liq}, n_{gas}]^\top \in \mathbb{R}^{2N+5}
\end{equation}

各状态变量含义如下：
\begin{itemize}
  \item $T_{s,in}$：换热器碱液出口温度 [K]
  \item $T_{s,i}$：第$i$个电解槽温度 [K]，$i=1,\ldots,N$
  \item $T_{sep}$：分离器温度 [K]
  \item $T_{c,out}$：冷却水出口温度 [K]
  \item $n_{H_2,an,i}$：第$i$个阳极室溶解氢气摩尔数 [mol]
  \item $n_{liq}$：分离器液相氢气摩尔数 [mol]
  \item $n_{gas}$：分离器气相氢气摩尔数 [mol]
\end{itemize}

控制输入为$(2N+1)$维：
\begin{equation}
  \mathbf{u} = [I_1, \ldots, I_N, v_{lye,1}, \ldots, v_{lye,N}, v_c]^\top \in \mathbb{R}^{2N+1}
\end{equation}

其中$I_i$为第$i$个电解槽电流 [A]，$v_{lye,i}$为碱液流量 [$\mathrm{m}^3/\mathrm{s}$]，$v_c$为冷却水流量 [$\mathrm{m}^3/\mathrm{s}$]。

系统动力学可统一表示为：
\begin{equation}
  \dot{\mathbf{x}} = \mathbf{f}(\mathbf{x}, \mathbf{u}), \quad \mathbf{x} \in \mathbb{R}^{2N+5}, \, \mathbf{u} \in \mathbb{R}^{2N+1}
\end{equation}

上述状态空间模型可归纳如表~\ref{tab:state_space_summary} 所示。
系统包含四个功能子模型：电化学与产气模型（代数方程）、碱液流分配模型（代数方程）、传热模型（$N+3$阶ODE）和HTO杂质累积模型（$N+2$阶ODE）。

% N-in-1 AWE系统状态空间模型总结表格
\begin{table}[!htbp]
  \centering
  \caption{$N$槽并联AWE系统状态空间模型总结}
  \label{tab:state_space_summary}
  \small
  \begin{tabular}{@{}c c c c c@{}}
    \toprule
    \textbf{子模型} & \textbf{电化学与产气} & \textbf{碱液流分配} & \textbf{传热} & \textbf{HTO杂质累积} \\
    \midrule
    \multirow{2}{*}{状态变量} & \multirow{2}{*}{/} & \multirow{2}{*}{/} & $T_{s,in}$, & $\{n_{H_2,an,i}\}_{i=1}^N$, \\
    & & & $\{T_{s,i}\}_{i=1}^N$, $T_{sep}$, $T_{c,out}$ & $n_{liq}$, $n_{gas}$ \\
    \addlinespace
    模型阶数 & 0 & 0 & $N+3$ & $N+2$ \\
    \midrule
    \multicolumn{5}{l}{\textbf{控制变量：} 电解电流 $\{I_i\}_{i=1}^N$，碱液流量 $\{v_{lye,i}\}_{i=1}^N$，冷却水流量 $v_c$} \\
    \bottomrule
  \end{tabular}
\end{table}

表~\ref{tab:state_space_summary} 系统化了各子模型与状态变量之间的对应关系。
四槽系统（$N=4$）的总状态维度为$2N+5=13$，控制维度为$2N+1=9$。

\section{仿真平台架构}

为验证所提控制策略的有效性，搭建了涵盖物理仿真、专家数据生成、策略训练与闭环验证的完整实验平台。该平台由五个功能模块组成，各模块的职责划分与数据流转关系如下。

\subsection{功能模块划分}

仿真平台由五个功能模块协同构成：物理仿真引擎负责基于电化学、热力学与氢气传输模型，使用SciPy \texttt{solve\_ivp}实现多时间尺度ODE积分，模拟四槽并联AWE系统的闭环动态响应；NMPC专家数据生成模块基于CasADi/IPOPT求解器，在每个控制周期内求解非线性优化问题，生成覆盖多工况的状态-动作专家轨迹，作为后续策略学习的监督标签；策略训练模块基于PyTorch实现TCN Diffusion网络，采用DDPM训练范式，提供数据归一化、训练/测试集划分与模型检查点保存功能；安全约束处理模块实现投影算子单步预测修正与CBF李导数安全滤波，将生成模型的无约束输出映射到严格满足温度、HTO、电压与功率约束的可行控制集；闭环验证与评估模块则在典型与极端工况下开展闭环仿真，记录功率跟踪误差、温度调节偏差、约束违反次数与计算耗时，生成对比图表与统计报告。

